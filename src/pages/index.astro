---
import BaseLayout from '../layouts/BaseLayout.astro';
import AdSlot from '../components/AdSlot.astro';
import ArticleCard from '../components/ArticleCard.astro';
import ToolCard from '../components/ToolCard.astro';
import HeroMedia from '../components/HeroMedia.astro';
import PhotoGallery from '../components/PhotoGallery.astro';
import VideoShowcase from '../components/VideoShowcase.astro';
import CategoryCard from '../components/CategoryCard.astro';
import SectionHeading from '../components/SectionHeading.astro';
import ScrollReveal from '../components/ScrollReveal.astro';
import DailyTip from '../components/DailyTip.astro';
import { getCollection } from 'astro:content';
import { SITE, CATEGORIES, TRUST_POINTS } from '../lib/site';
import { TOOL_IMAGES, CATEGORY_IMAGES } from '../lib/media';
import { TOOLS } from '../lib/tools';

const articles = (await getCollection('articles'))
  .sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf());
const featured = articles.filter((a) => a.data.featured).slice(0, 3);
const latest = articles.slice(0, 6);
const articleCount = articles.length;

const toolImageKeys: Record<string, keyof typeof TOOL_IMAGES> = {
  '/tools/bmi-calculator': 'bmi',
  '/tools/water-intake-calculator': 'water',
  '/tools/protein-calculator': 'protein',
  '/tools/food-calorie-calculator': 'foodCalories',
  '/tools/walking-calorie-calculator': 'walking',
  '/tools/bedtime-calculator': 'bedtime',
  '/tools/target-heart-rate-calculator': 'heartRate',
  '/tools/step-goal-calculator': 'steps',
  '/tools/7-day-walking-plan': 'walkingPlan',
  '/tools/7-day-hydration-plan': 'water',
  '/tools/7-day-sleep-plan': 'bedtime',
  '/tools/habit-tracker': 'habits',
};
---

<BaseLayout>
  <HeroMedia />

  <!-- Trust bar -->
  <section class="border-y border-sage-100 bg-white/80 backdrop-blur-sm">
    <div class="mx-auto flex max-w-6xl flex-wrap items-center justify-center gap-x-10 gap-y-3 px-4 py-5 sm:px-6">
      {
        TRUST_POINTS.map((point, i) => (
          <span
            class="trust-item flex items-center gap-2 text-sm font-medium text-ink-soft"
            style={`animation-delay: ${i * 100}ms`}
          >
            <svg class="h-4 w-4 text-sage-600" fill="currentColor" viewBox="0 0 20 20">
              <path
                fill-rule="evenodd"
                d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                clip-rule="evenodd"
              />
            </svg>
            {point}
          </span>
        ))
      }
    </div>
  </section>

  <!-- Calculators promo — above the fold -->
  <section class="border-b border-sage-100 bg-gradient-to-br from-sage-800 to-sage-900 py-12 lg:py-14">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <div class="flex flex-col items-center text-center">
        <p class="text-sm font-semibold uppercase tracking-wide text-gold-light">No signup · Instant results</p>
        <h2 class="font-display mt-2 text-3xl font-bold text-white sm:text-4xl">Use our free health calculators</h2>
        <p class="mt-3 max-w-2xl text-lg text-white/85">
          BMI, protein, food nutrition with barcode scan, water intake, sleep, walking, and more — {TOOLS.length} tools, all free.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-3">
          <a href="/tools/food-calorie-calculator" class="btn-primary !bg-white !text-sage-900 hover:!bg-cream">
            Food nutrition calculator
          </a>
          <a href="/tools/protein-calculator" class="rounded-full border border-white/30 bg-white/10 px-6 py-3 text-sm font-semibold text-white transition hover:bg-white/20">
            Protein calculator
          </a>
          <a href="/tools/bmi-calculator" class="rounded-full border border-white/30 bg-white/10 px-6 py-3 text-sm font-semibold text-white transition hover:bg-white/20">
            BMI calculator
          </a>
          <a href="/tools" class="rounded-full border border-white/30 px-6 py-3 text-sm font-semibold text-white/90 transition hover:text-white">
            All {TOOLS.length} tools →
          </a>
        </div>
      </div>
    </div>
  </section>

  <div class="py-6">
    <DailyTip />
  </div>

  <AdSlot slot="3333333333" format="horizontal" class="mx-auto max-w-6xl px-4 pt-4" />

  <!-- Categories with photos -->
  <section class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:py-20">
    <ScrollReveal>
      <SectionHeading
        label="Explore topics"
        title="What would you like to feel better about?"
        description="Real photo guides on nutrition, movement, sleep, and more — steps you can take today."
      />
    </ScrollReveal>
    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
      {CATEGORIES.map((cat, i) => (
        <ScrollReveal delay={i * 80}>
          <CategoryCard slug={cat.slug} />
        </ScrollReveal>
      ))}
    </div>
  </section>

  <PhotoGallery />

  <VideoShowcase />

  <!-- Featured -->
  {
    featured.length > 0 && (
      <section class="bg-sage-900 py-16 lg:py-20">
        <div class="mx-auto max-w-6xl px-4 sm:px-6">
          <ScrollReveal>
            <SectionHeading
              label="Editor's picks"
              title="Start with these guides"
              description="Our most helpful reads for staying active, eating well, and protecting your heart."
              dark
            />
          </ScrollReveal>
          <div class="grid gap-6 md:grid-cols-3">
            {featured.map((article, i) => (
              <ScrollReveal delay={i * 100}>
                <ArticleCard article={article} featured />
              </ScrollReveal>
            ))}
          </div>
        </div>
      </section>
    )
  }

  <!-- Latest -->
  <section class="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:py-20">
    <ScrollReveal>
      <div class="mb-10 flex flex-wrap items-end justify-between gap-4">
        <SectionHeading label="Fresh reads" title="Latest articles" />
        <a
          href="/articles"
          class="inline-flex items-center gap-1 text-sm font-semibold text-sage-700 hover:text-sage-900"
        >
          View all {articleCount} articles
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
          </svg>
        </a>
      </div>
    </ScrollReveal>
    <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
      {latest.map((article, i) => (
        <ScrollReveal delay={i * 60}>
          <ArticleCard article={article} />
        </ScrollReveal>
      ))}
    </div>
  </section>

  <!-- Tools grid — all calculators -->
  <section class="border-t border-sage-100 bg-gradient-to-b from-cream-dark to-cream py-16 lg:py-20">
    <div class="mx-auto max-w-6xl px-4 sm:px-6">
      <ScrollReveal>
        <SectionHeading
          label="Free tools"
          title="Every calculator & planner"
          description="Pick one to get started — each tool links to related ones so you can build a full daily wellness picture."
          centered
        />
      </ScrollReveal>
      <div class="mb-8 flex flex-wrap justify-center gap-4">
        <a href="/tools/food-calorie-calculator" class="btn-primary !inline-flex">Try food nutrition calculator</a>
        <a href="/start" class="inline-flex items-center gap-2 text-sm font-semibold text-sage-700 hover:text-sage-900">
          Get a personalized plan →
        </a>
      </div>
      <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
        {
          TOOLS.map((tool, i) => {
            const key = toolImageKeys[tool.href] ?? 'bmi';
            const img = TOOL_IMAGES[key];
            return (
              <ScrollReveal delay={i * 40}>
                <ToolCard
                  title={tool.title}
                  description={tool.description}
                  href={tool.href}
                  image={img.src}
                  imageAlt={img.alt}
                />
              </ScrollReveal>
            );
          })
        }
      </div>
    </div>
  </section>

  <!-- CTA with background photo -->
  <section class="mx-auto max-w-6xl px-4 py-16 sm:px-6">
    <ScrollReveal>
      <div class="relative overflow-hidden rounded-3xl shadow-2xl">
        <img
          src={CATEGORY_IMAGES.wellness.src.replace('w=800', 'w=1600')}
          alt=""
          class="absolute inset-0 h-full w-full object-cover"
          aria-hidden="true"
          loading="lazy"
        />
        <div class="absolute inset-0 bg-gradient-to-r from-sage-900/95 to-sage-900/75" />
        <div class="relative px-8 py-14 text-center sm:px-16 sm:py-16">
          <p class="section-label !text-gold-light">Stay informed</p>
          <h2 class="font-display mx-auto max-w-xl text-3xl font-bold text-white sm:text-4xl">
            Health is a daily habit — we're here every step
          </h2>
          <p class="mx-auto mt-4 max-w-lg text-sage-200">
            Bookmark {SITE.name} and check back for new guides. Your future self will thank you.
          </p>
          <a href="/tools" class="btn-primary mt-8 !bg-white !text-sage-800 !shadow-none hover:!bg-cream">
            Explore all calculators
          </a>
        </div>
      </div>
    </ScrollReveal>
  </section>
</BaseLayout>
